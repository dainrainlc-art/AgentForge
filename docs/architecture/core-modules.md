# AgentForge 核心模块架构设计

## 1. AgentForge Core (核心调度模块)

### 1.1 模块概述

AgentForge Core是系统的核心调度中心，负责意图识别、任务路由、多Agent协调和记忆管理。

### 1.2 架构设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          AgentForge Core                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         请求入口 (Request Gateway)                     │  │
│  │                                                                        │  │
│  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │  │
│  │   │ HTTP API   │  │ WebSocket   │  │    CLI     │  │  Webhook    │  │  │
│  │   └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │  │
│  │          │                │                │                │          │  │
│  │          └────────────────┴────────────────┴────────────────┘          │  │
│  │                                     │                                   │  │
│  └─────────────────────────────────────┼───────────────────────────────────┘  │
│                                        ▼                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         意图识别器 (Intent Recognizer)                 │  │
│  │                                                                        │  │
│  │   输入: 用户请求/事件                                                  │  │
│  │   处理: GLM-5分析意图                                                  │  │
│  │   输出: 意图类型 + 参数提取                                            │  │
│  │                                                                        │  │
│  │   意图类型:                                                            │  │
│  │   ├── fiverr_order: Fiverr订单相关                                    │  │
│  │   ├── content_creation: 内容创作                                      │  │
│  │   ├── social_media: 社交媒体                                          │  │
│  │   ├── knowledge: 知识管理                                             │  │
│  │   ├── communication: 客户沟通                                         │  │
│  │   └── system: 系统操作                                                │  │
│  │                                                                        │  │
│  └─────────────────────────────────────┬───────────────────────────────────┘  │
│                                        ▼                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         任务路由器 (Task Router)                       │  │
│  │                                                                        │  │
│  │   根据意图类型选择执行路径:                                            │  │
│  │                                                                        │  │
│  │   fiverr_order ──────► Fiverr运营引擎                                 │  │
│  │   content_creation ──► 内容生成引擎 ──► GLM-5                         │  │
│  │   social_media ──────► 社交媒体引擎 ──► N8N                           │  │
│  │   knowledge ─────────► 知识管理引擎                                   │  │
│  │   communication ────► 客户沟通引擎 ──► GLM-5                          │  │
│  │   system ───────────► 系统管理模块                                    │  │
│  │                                                                        │  │
│  └─────────────────────────────────────┬───────────────────────────────────┘  │
│                                        ▼                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                      多Agent协调器 (Agent Coordinator)                 │  │
│  │                                                                        │  │
│  │   协调模式:                                                            │  │
│  │   ├── 主导-支持模式: 主Agent主导，其他Agent支持                        │  │
│  │   ├── 并行模式: 多Agent独立执行                                       │  │
│  │   └── 串行模式: Agent按顺序执行                                       │  │
│  │                                                                        │  │
│  │   状态管理:                                                            │  │
│  │   ├── 任务状态追踪                                                    │  │
│  │   ├── Agent负载均衡                                                   │  │
│  │   └── 冲突检测与解决                                                  │  │
│  │                                                                        │  │
│  └─────────────────────────────────────┬───────────────────────────────────┘  │
│                                        ▼                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         记忆管理器 (Memory Manager)                    │  │
│  │                                                                        │  │
│  │   ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │   │                    记忆存储层                                    │  │  │
│  │   │                                                                  │  │  │
│  │   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │  │  │
│  │   │   │ 短期记忆   │  │ 工作记忆   │  │       长期记忆          │ │  │  │
│  │   │   │  (Redis)   │  │ (Session)  │  │ (PostgreSQL + Qdrant)  │ │  │  │
│  │   │   └─────────────┘  └─────────────┘  └─────────────────────────┘ │  │  │
│  │   │                                                                  │  │  │
│  │   └─────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                        │  │
│  │   自进化机制:                                                          │  │
│  │   ├── 每日03:00 记忆巩固 (去重、提取洞察)                             │  │
│  │   ├── 每日04:00 自我检查 (错误分析、自动修复)                         │  │
│  │   └── 任务完成 自动复盘 (经验沉淀)                                    │  │
│  │                                                                        │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 接口定义

```python
class AgentForgeCore:
    """AgentForge核心调度模块"""
    
    async def process_request(
        self, 
        request: Request
    ) -> Response:
        """处理用户请求"""
        pass
    
    async def recognize_intent(
        self, 
        input: str
    ) -> IntentResult:
        """识别用户意图"""
        pass
    
    async def route_task(
        self, 
        intent: IntentResult
    ) -> TaskResult:
        """路由任务到对应模块"""
        pass
    
    async def coordinate_agents(
        self, 
        task: Task,
        mode: CoordinationMode
    ) -> CoordinationResult:
        """协调多Agent执行"""
        pass
    
    async def update_memory(
        self, 
        memory_type: MemoryType,
        content: Any
    ) -> None:
        """更新记忆"""
        pass
```

## 2. GLM-5 Integration (AI能力模块)

### 2.1 模块概述

GLM-5 Integration负责与百度千帆GLM-5模型的集成，包括API调用、Prompt管理、响应解析和配额监控。

### 2.2 架构设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          GLM-5 Integration                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                       API调用封装层 (API Client)                       │  │
│  │                                                                        │  │
│  │   ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │   │                    HTTP Client                                  │  │  │
│  │   │                                                                  │  │  │
│  │   │   Base URL: https://qianfan.baidubce.com/v2/coding              │  │  │
│  │   │   Auth: Bearer Token (API Key)                                  │  │  │
│  │   │   Timeout: 60s                                                  │  │  │
│  │   │   Retry: 3次，指数退避                                          │  │  │
│  │   │                                                                  │  │  │
│  │   └─────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                        │  │
│  └─────────────────────────────────────┬───────────────────────────────────┘  │
│                                        ▼                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    Prompt模板管理器 (Prompt Manager)                   │  │
│  │                                                                        │  │
│  │   模板类型:                                                            │  │
│  │   ├── system_prompts/    # 系统提示词                                 │  │
│  │   │   ├── architect.yaml                                            │  │
│  │   │   ├── ai_engineer.yaml                                          │  │
│  │   │   └── ...                                                       │  │
│  │   ├── skill_prompts/     # 技能提示词                                │  │
│  │   │   ├── fiverr_order.yaml                                         │  │
│  │   │   ├── content_creation.yaml                                     │  │
│  │   │   └── ...                                                       │  │
│  │   └── task_prompts/      # 任务提示词                                │  │
│  │       ├── customer_reply.yaml                                       │  │
│  │       ├── social_post.yaml                                          │  │
│  │       └── ...                                                       │  │
│  │                                                                        │  │
│  │   模板变量: {{variable}}                                              │  │
│  │                                                                        │  │
│  └─────────────────────────────────────┬───────────────────────────────────┘  │
│                                        ▼                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    响应解析器 (Response Parser)                        │  │
│  │                                                                        │  │
│  │   解析流程:                                                            │  │
│  │   1. JSON解析                                                         │  │
│  │   2. 内容提取                                                         │  │
│  │   3. 格式验证                                                         │  │
│  │   4. 错误检测                                                         │  │
│  │                                                                        │  │
│  │   输出格式:                                                            │  │
│  │   ├── text: 纯文本                                                    │  │
│  │   ├── json: JSON对象                                                  │  │
│  │   ├── markdown: Markdown文档                                          │  │
│  │   └── structured: 结构化数据                                          │  │
│  │                                                                        │  │
│  └─────────────────────────────────────┬───────────────────────────────────┘  │
│                                        ▼                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    配额监控器 (Quota Monitor)                          │  │
│  │                                                                        │  │
│  │   监控维度:                                                            │  │
│  │   ├── 每5小时请求量 (限制: 6000)                                      │  │
│  │   ├── 每周请求量 (限制: 45000)                                        │  │
│  │   ├── 每月请求量 (限制: 90000)                                        │  │
│  │   └── 联网搜索次数 (限制: 2000/月)                                    │  │
│  │                                                                        │  │
│  │   告警级别:                                                            │  │
│  │   ├── 80%: 保守模式 (暂停P2任务)                                      │  │
│  │   ├── 95%: 紧急模式 (仅P0任务)                                        │  │
│  │   └── 100%: 配额耗尽告警                                              │  │
│  │                                                                        │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.3 接口定义

```python
class GLM5Integration:
    """GLM-5集成模块"""
    
    async def generate(
        self, 
        prompt: str,
        context: Optional[dict] = None,
        **kwargs
    ) -> GenerationResult:
        """生成内容"""
        pass
    
    async def chat(
        self, 
        messages: List[Message],
        **kwargs
    ) -> ChatResult:
        """多轮对话"""
        pass
    
    async def use_template(
        self, 
        template_name: str,
        variables: dict,
        **kwargs
    ) -> GenerationResult:
        """使用模板生成"""
        pass
    
    def get_quota_status(self) -> QuotaStatus:
        """获取配额状态"""
        pass
```

## 3. N8N Workflow Engine (工作流引擎)

### 3.1 模块概述

N8N Workflow Engine负责工作流定义、执行、事件监听和定时任务调度。

### 3.2 工作流架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        N8N Workflow Engine                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                      工作流定义层 (Workflow Definition)                │  │
│  │                                                                        │  │
│  │   workflows/                                                           │  │
│  │   ├── fiverr/                                                         │  │
│  │   │   ├── order-monitor.json          # 订单监控                      │  │
│  │   │   ├── message-handler.json        # 消息处理                      │  │
│  │   │   └── delivery-processor.json     # 交付处理                      │  │
│  │   ├── social/                                                         │  │
│  │   │   ├── content-publisher.json      # 内容发布                      │  │
│  │   │   └── engagement-tracker.json     # 互动追踪                      │  │
│  │   ├── knowledge/                                                      │  │
│  │   │   ├── obsidian-sync.json          # Obsidian同步                  │  │
│  │   │   └── notion-sync.json            # Notion同步                    │  │
│  │   └── scheduled/                                                      │  │
│  │       ├── daily-report.json           # 每日报告                      │  │
│  │       └── backup.json                 # 自动备份                      │  │
│  │                                                                        │  │
│  └─────────────────────────────────────┬───────────────────────────────────┘  │
│                                        ▼                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                      执行引擎层 (Execution Engine)                     │  │
│  │                                                                        │  │
│  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │  │
│  │   │ 触发器管理 │  │ 节点执行器 │  │ 数据转换器 │  │ 错误处理器 │  │  │
│  │   └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘  │  │
│  │                                                                        │  │
│  │   执行模式:                                                            │  │
│  │   ├── 同步执行: 阻塞等待结果                                          │  │
│  │   ├── 异步执行: 后台执行，回调通知                                    │  │
│  │   └── 定时执行: 按计划执行                                            │  │
│  │                                                                        │  │
│  └─────────────────────────────────────┬───────────────────────────────────┘  │
│                                        ▼                                      │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                      事件监听层 (Event Listener)                       │  │
│  │                                                                        │  │
│  │   事件源:                                                              │  │
│  │   ├── Webhook: 接收外部事件                                           │  │
│  │   ├── Schedule: 定时触发                                              │  │
│  │   ├── File Watch: 文件变更                                            │  │
│  │   └── Manual: 手动触发                                                │  │
│  │                                                                        │  │
│  │   事件处理:                                                            │  │
│  │   ├── 事件验证                                                        │  │
│  │   ├── 事件分类                                                        │  │
│  │   └── 工作流触发                                                      │  │
│  │                                                                        │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 与GLM-5协同接口

```python
class N8NGLM5Bridge:
    """N8N与GLM-5协同桥接"""
    
    async def call_glm5(
        self, 
        workflow_id: str,
        prompt_data: dict
    ) -> dict:
        """从N8N调用GLM-5"""
        pass
    
    async def trigger_workflow(
        self, 
        workflow_name: str,
        trigger_data: dict
    ) -> str:
        """从GLM-5触发N8N工作流"""
        pass
    
    async def get_execution_status(
        self, 
        execution_id: str
    ) -> ExecutionStatus:
        """获取工作流执行状态"""
        pass
```

## 4. 业务模块架构

### 4.1 Fiverr运营引擎

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Fiverr运营引擎                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  订单监控器  │  │  报价生成器  │  │  消息处理器  │  │  交付管理器  │    │
│  │              │  │              │  │              │  │              │    │
│  │ • 实时轮询   │  │ • 成本分析   │  │ • 意图识别   │  │ • 文件打包   │    │
│  │ • 状态追踪   │  │ • 方案生成   │  │ • 回复生成   │  │ • 文档生成   │    │
│  │ • 事件触发   │  │ • 价格优化   │  │ • 翻译优化   │  │ • 版本管理   │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 社交媒体引擎

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        社交媒体引擎                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  内容生成器  │  │  平台适配器  │  │  发布调度器  │  │  效果分析器  │    │
│  │              │  │              │  │              │  │              │    │
│  │ • 主题策划   │  │ • 格式转换   │  │ • 定时发布   │  │ • 数据收集   │    │
│  │ • 文案生成   │  │ • 长度适配   │  │ • 多平台分发 │  │ • 效果评估   │    │
│  │ • 配图建议   │  │ • 标签优化   │  │ • 状态追踪   │  │ • 策略优化   │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
│                                                                              │
│  支持平台: YouTube | Twitter | Facebook | Instagram | LinkedIn             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.3 知识管理引擎

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        知识管理引擎                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  Obsidian    │  │  Notion      │  │  同步管理器  │  │  检索引擎    │    │
│  │  本地管理器  │  │  云端管理器  │  │              │  │              │    │
│  │              │  │              │  │              │  │              │    │
│  │ • 文件监控   │  │ • API集成    │  │ • 双向同步   │  │ • 语义搜索   │    │
│  │ • 模板管理   │  │ • 数据库操作 │  │ • 冲突检测   │  │ • 标签索引   │    │
│  │ • 链接解析   │  │ • 页面管理   │  │ • 版本控制   │  │ • 推荐系统   │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```
